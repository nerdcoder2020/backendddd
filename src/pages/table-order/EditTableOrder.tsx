import { Grid } from '@mui/material';
import EditTableOrder from 'components/sections/dashboard/table-order/edittableorder';

const editorder = () => {
  return (
    <Grid container spacing={2}>
      <EditTableOrder />
    </Grid>
  );
};

export default editorder;
