import { Grid } from '@mui/material';
import SettingPage from 'components/sections/dashboard/setting/settingpage';

const settingpage = () => {
  return (
    <Grid container spacing={2}>
      <SettingPage />
    </Grid>
  );
};

export default settingpage;
