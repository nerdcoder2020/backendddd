import { Grid } from '@mui/material';
import ItemReport from 'components/sections/dashboard/sales/itemreport';

const Itemreport = () => {
  return (
    <Grid container spacing={2}>
      <ItemReport />
    </Grid>
  );
};

export default Itemreport;
