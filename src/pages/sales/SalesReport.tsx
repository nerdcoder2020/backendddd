import { Grid } from '@mui/material';
import SalesReport from 'components/sections/dashboard/sales/salesreport';

const Salesreport = () => {
  return (
    <Grid container spacing={2}>
      <SalesReport />
    </Grid>
  );
};

export default Salesreport;
