import { Grid } from '@mui/material';
import AddEmployeePage from 'components/sections/dashboard/employee/addemployee';

const AddEmployee = () => {
  return (
    <Grid container spacing={2}>
      <AddEmployeePage />
    </Grid>
  );
};

export default AddEmployee;
