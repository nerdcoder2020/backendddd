import { Grid } from '@mui/material';
import ContactUS from 'components/sections/dashboard/contactus/contactus';

const contactus = () => {
  return (
    <Grid container spacing={2}>
      <ContactUS />
    </Grid>
  );
};

export default contactus;
